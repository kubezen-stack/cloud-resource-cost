apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cost-optimizer.fullname" . }}-test-connection"
  labels:
    {{- include "cost-optimizer.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cost-optimizer.fullname" . }}:{{ .Values.service.port }}/health']
  restartPolicy: Never
